# Copy this file to .env and fill in your key. Do not commit .env.

# Server
PORT=3001
CLIENT_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175
APP_URL=http://localhost:5173

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/emotional_ai

# JWT & sessions
JWT_ACCESS_SECRET=replace-me-with-a-long-random-string
ACCESS_TOKEN_TTL=900
REFRESH_TOKEN_TTL_DAYS=30
BCRYPT_SALT_ROUNDS=10
REFRESH_TOKEN_COOKIE_NAME=refreshToken

# MFA
MFA_ISSUER=Anchor

# Email (password reset). Leave blank to log reset links to the console.
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Anthropic (Claude) API key — server side only
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5-20251001
